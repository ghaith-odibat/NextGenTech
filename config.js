// Storefront configuration.
//
// apiBaseUrl points at the Express server in backend/. Leave it empty to run the
// storefront standalone (e.g. on GitHub Pages), where checkout confirms the order
// without persisting it.
window.NEXTGENTECH_CONFIG = {
  apiBaseUrl: '',
};
